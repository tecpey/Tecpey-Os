"use client";

type Props = {
  activeFilter: string;
  onFilterChange: (filter: string) => void;
  t: (key: string) => string;
};

export default function MarketsFilters(_: Props) {
  return null;
}

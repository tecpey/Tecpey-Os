# Temporary Bootstrap Assets

The following files are temporary and must not merge:

- `.github/workflows/api-security-manifest-bootstrap.yml`
- `docs/security/API_SECURITY_MANIFEST_BOOTSTRAP.md`

They exist only to capture the first deterministic generated inventory as a workflow artifact.

# Decision: Generated Mutating API Manifest

TecPey will govern mutating API coverage from source-derived evidence rather than a manually maintained route list. The committed snapshot remains reviewable and deterministic, while CI regeneration prevents omissions and stale classifications.

# Temporary transfer notice

This file exists only to force a final pull-request synchronization event for the reviewed Academy authority patch applicator. The one-shot workflow removes this notice, all encoded patch parts, and itself before committing the real implementation. It must never be merged into `main`.

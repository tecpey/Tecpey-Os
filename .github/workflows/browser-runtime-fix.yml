name: Browser Runtime Fix

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/browser-runtime-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    name: Apply CSP and locale runtime fixes
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref_name }}
      - name: Apply staged fixes
        run: python3 scripts/apply-browser-runtime-fixes.py
      - name: Commit non-workflow fixes
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref || github.ref_name }}
        run: |
          git checkout HEAD -- .github/workflows/browser-runtime-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(ui): restore hydration and server locale semantics"
          git push origin "HEAD:${HEAD_REF}"

+  type NewsGrowthAuthority,
+} from "./news-growth-authority";
+ 
+ export type NewsSourceTier = "official" | "trusted_media" | "tecpey_editorial" | "watchlist";
+ 
+@@ -33,6 +40,13 @@ export type RawNewsInput = {
+   url: string;
+   publishedAt: string;
+   fetchedAt: string;
++  originalTitle?: string;
++  originalSummary?: string;
++  sourceBody?: string;
++  translatedTitle?: string;
++  translatedLead?: string;
++  translatedBody?: string;
++  translationStatus?: "not_required" | "translated" | "pending" | "failed";
+ };
+ 
+ export type NormalizedNewsArticle = {
+@@ -50,10 +64,18 @@ export type NormalizedNewsArticle = {
+   tone: NewsImpactTone;
+   detectedCoins: string[];
+   detectedTools: string[];
++  topicTags: string[];
+   relatedLessonHref: string;
+   impactScore: number;
+   priority: number;
+   idempotencyKey: string;
++  originalTitle?: string;
++  originalSummary?: string;
++  sourceBody?: string;
++  translatedTitle?: string;
++  translatedLead?: string;
++  translatedBody?: string;
++  translationStatus: "not_required" | "translated" | "pending" | "failed";
+ };
+ 
+ export type NewsAutomationRejectReason =
+@@ -65,7 +87,9 @@ export type NewsAutomationRejectReason =
+   | "prohibited_financial_advice"
+   | "hype_or_profit_promise"
+   | "no_supported_entity"
+-  | "missing_seo_schema";
++  | "missing_seo_schema"
++  | "missing_growth_authority"
++  | "translation_unavailable";
+ 
+ export type NewsAutomationDecisionStatus = "publishable" | "needs_review" | "rejected";
+ 
+@@ -79,6 +103,7 @@ export type NewsAutomationDecision = {
+   relations: EntityRelation[];
+   coinImpacts: CoinPriorityResult[];
+   historyItems: NewsImpactHistoryItem[];
++  growthAuthority: NewsGrowthAuthority;
+ };
+ 
+ type EntityDictionary = {
+@@ -103,6 +128,21 @@ const ENTITY_DICTIONARY: EntityDictionary = {
+     TON: ["toncoin", "ton", "telegram open network", "تون", "تون کوین", "تون‌کوین"],
+     SOL: ["solana", "sol", "سولانا"],
+     DOGE: ["dogecoin", "doge", "دوج کوین", "دوج‌کوین"],
++    BNB: ["bnb", "binance coin", "بی ان بی", "بی‌ان‌بی"],
++    XRP: ["xrp", "ripple", "ریپل"],
++    ADA: ["cardano", "ada", "کاردانو"],
++    TRX: ["tron", "trx", "ترون"],
++    AVAX: ["avalanche", "avax", "آوالانچ"],
++    LINK: ["chainlink", "link", "چین لینک", "چین‌لینک"],
++    DOT: ["polkadot", "dot", "پولکادات"],
++    LTC: ["litecoin", "ltc", "لایت کوین", "لایت‌کوین"],
++    ATOM: ["cosmos", "atom", "کازموس"],
++    XLM: ["stellar", "xlm", "استلار"],
++    UNI: ["uniswap", "uni", "یونی سواپ", "یونی‌سواپ"],
++    ...Object.fromEntries(coinGrowthCandidates.map((coin) => [
++      coin.symbol,
++      [coin.symbol, coin.name, coin.faName, coin.slug, ...coin.narrative],
++    ])),
+   },
+   tools: {
+     tradingview: ["tradingview", "chart", "technical analysis", "نمودار", "تحلیل تکنیکال"],
+@@ -290,6 +330,7 @@ function buildNewsOrganicGrowthProfile(article: NormalizedNewsArticle): OrganicG
+     `tone:${article.tone}`,
+     ...article.detectedCoins.map((symbol) => `coin:${symbol.toLowerCase()}`),
+     ...article.detectedTools.map((slug) => `tool:${slug}`),
++    ...article.topicTags,
+   ];
+   const safetyDisclaimer = isEn
+     ? "This automated news page is not financial advice, a trading signal or a profit promise."
+@@ -309,6 +350,7 @@ function buildNewsOrganicGrowthProfile(article: NormalizedNewsArticle): OrganicG
+       "market context",
+       ...article.detectedCoins,
+       ...article.detectedTools,
++      ...article.topicTags.map((tag) => tag.replace(/^topic:/, "")),
+     ],
+     entityTags,
+     internalLinks: [
+@@ -372,7 +414,11 @@ function reviewReasons(input: RawNewsInput, article: NormalizedNewsArticle, sour
+   if (hoursBetween(article.publishedAt, article.recordedAt) > 168) reasons.push("stale_news");
+   if (containsAny(text, PROHIBITED_ADVICE_PATTERNS)) reasons.push("prohibited_financial_advice");
+   if (containsAny(text, HYPE_PATTERNS)) reasons.push("hype_or_profit_promise");
+-  if (article.detectedCoins.length === 0 && article.detectedTools.length === 0) reasons.push("no_supported_entity");
++  const meaningfulTopicCount = article.topicTags.filter((tag) => tag.startsWith("topic:") && tag !== "topic:crypto").length;
++  if (article.detectedCoins.length === 0 && article.detectedTools.length === 0 && meaningfulTopicCount === 0) {
++    reasons.push("no_supported_entity");
++  }
++  if (article.locale === "fa" && article.translationStatus !== "translated") reasons.push("translation_unavailable");
+   return reasons;
+ }
+ 
+@@ -386,6 +432,7 @@ export function normalizeNewsInput(
+   const combined = `${title} ${summary}`;
+   const detectedCoins = detectEntities(combined, ENTITY_DICTIONARY.coins);
+   const detectedTools = detectEntities(combined, ENTITY_DICTIONARY.tools);
++  const topicTags = buildNewsTopicTags(combined, detectedCoins, detectedTools);
+   const idempotencyKey = stableHash(`${input.locale}|${input.url}|${input.publishedAt}|${title}`);
+   const slug = `${slugify(title).slice(0, 72)}-${idempotencyKey.slice(0, 6)}`;
+   const impactScore = baseImpactScore(combined, detectedCoins.length, detectedTools.length);
+@@ -395,6 +442,13 @@ export function normalizeNewsInput(
+     slug,
+     title,
+     summary,
++    originalTitle: input.originalTitle,
++    originalSummary: input.originalSummary,
++    sourceBody: input.sourceBody,
++    translatedTitle: input.translatedTitle,
++    translatedLead: input.translatedLead,
++    translatedBody: input.translatedBody,
++    translationStatus: input.translationStatus ?? (input.locale === "fa" ? "pending" : "not_required"),
+     sourceName: input.sourceName.trim() || source?.name || "Unknown source",
+     sourceUrl: input.sourceUrl,
+     canonicalUrl: input.url,
+@@ -404,6 +458,7 @@ export function normalizeNewsInput(
+     tone: inferTone(combined),
+     detectedCoins,
+     detectedTools,
++    topicTags,
+     relatedLessonHref: inferRelatedLessonHref(input, combined),
+     impactScore,
+     priority: 0,
+@@ -420,6 +475,21 @@ export function buildNewsAutomationDecision(
+   const article = normalizeNewsInput(input, sources);
+   const seo = buildSeo(article);
+   const organicGrowth = buildNewsOrganicGrowthProfile(article);
++  const growthAuthority = buildNewsGrowthAuthority({
++    locale: article.locale,
++    title: article.title,
++    summary: article.summary,
++    canonicalUrl: seo.canonical,
++    sourceName: article.sourceName,
++    sourceUrl: article.sourceUrl,
++    publishedAt: article.publishedAt,
++    recordedAt: article.recordedAt,
++    topicTags: article.topicTags,
++    detectedCoins: article.detectedCoins,
++    detectedTools: article.detectedTools,
++    relatedLessonHref: article.relatedLessonHref,
++    internalLinks: organicGrowth.internalLinks,
++  });
+   const contentItem: ContentItem = {
+     id: article.id,
+     type: "news",
+@@ -436,6 +506,7 @@ export function buildNewsAutomationDecision(
+   const reasons = reviewReasons(input, article, source);
+   if (!isPublishableContent(contentItem)) reasons.push("missing_seo_schema");
+   if (!isOrganicGrowthReadyContent(contentItem)) reasons.push("missing_seo_schema");
++  if (!validateNewsGrowthAuthority(growthAuthority)) reasons.push("missing_growth_authority");
+   const fatal = reasons.some((reason) =>
+     ["missing_required_field", "invalid_published_at", "prohibited_financial_advice", "hype_or_profit_promise"].includes(reason),
+   );
+@@ -463,6 +534,15 @@ export function buildNewsAutomationDecision(
+           locale: article.locale,
+           title: article.title,
+           summary: article.summary,
++          originalTitle: article.originalTitle,
++          originalSummary: article.originalSummary,
++          originalBody: article.sourceBody,
++          translatedTitle: article.translatedTitle,
++          translatedLead: article.translatedLead,
++          translatedBody: article.translatedBody,
++          translationStatus: article.translationStatus,
++          topicTags: article.topicTags,
++          growthAuthority,
+           sourceName: article.sourceName,
+           sourceUrl: article.sourceUrl,
+           newsUrl: newsDetailPath(article),
+@@ -487,6 +567,7 @@ export function buildNewsAutomationDecision(
+     contentItem,
+     seo,
+     organicGrowth,
++    growthAuthority,
+     relations,
+     coinImpacts,
+     historyItems,
+diff --git a/src/lib/news-detail-pages.ts b/src/lib/news-detail-pages.ts
+index 9b82b433..983fe366 100644
+--- a/src/lib/news-detail-pages.ts
++++ b/src/lib/news-detail-pages.ts
+@@ -11,7 +11,10 @@ import {
+   getNewsImpactSlugs,
+   type NewsImpactHistoryItem,
+ } from "./news-impact-history";
+-import { getNewsImpactHistoryItemsFromAuthority } from "./news-impact-history-authority";
++import {
++  getNewsImpactHistoryArchiveFromAuthority,
++  getNewsImpactHistoryItemsFromAuthority,
++} from "./news-impact-history-authority";
+ import { getTraderToolBySlug, type RankedTraderTool } from "./trading-tools-growth";
+ 
+ const SITE_URL = "https://tecpey.ir";
+@@ -29,7 +32,7 @@ export type NewsDetailPageModel = {
+   item: NewsImpactHistoryItem;
+   slug: string;
+   url: string;
+-  counterpartUrl: string;
++  counterpartUrl?: string;
+   relatedCoins: CoinPage[];
+   relatedTools: RankedTraderTool[];
+ };
+@@ -81,15 +84,13 @@ function buildNewsDetailPageModelFromItem(
+   const relatedTools = item.relatedToolSlugs
+     .map((toolSlug) => getTraderToolBySlug(toolSlug))
+     .filter((tool): tool is RankedTraderTool => Boolean(tool));
+-  const counterpartLocale: ContentLocale = locale === "en" ? "fa" : "en";
+-
+   return {
+     item,
+     slug,
+     url: `${SITE_URL}${getNewsImpactDetailPath(item)}`,
+     counterpartUrl: counterpart
+       ? `${SITE_URL}${getNewsImpactDetailPath(counterpart)}`
+-      : `${SITE_URL}${localePrefix(counterpartLocale)}/crypto-news/${slug}`,
++      : undefined,
+     relatedCoins,
+     relatedTools,
+   };
+@@ -117,7 +118,9 @@ export async function getNewsDetailPageModelFromAuthority(
+     getNewsImpactHistoryItemsFromAuthority(locale === "en" ? "fa" : "en"),
+   ]);
+   const item = items.find((entry) => getNewsImpactSlug(entry) === slug);
+-  const counterpart = counterpartItems.find((entry) => getNewsImpactSlug(entry) === slug);
++  const counterpart = counterpartItems.find((entry) =>
++    entry.sourceUrl === item?.sourceUrl || getNewsImpactSlug(entry) === slug
++  );
+   return buildNewsDetailPageModelFromItem(item, slug, locale, counterpart);
+ }
+ 
+@@ -129,7 +132,7 @@ export function getNewsDetailSitemapEntries() {
+ }
+ 
+ export async function getNewsDetailSitemapEntriesFromAuthority() {
+-  const items = await getNewsImpactHistoryItemsFromAuthority();
++  const items = await getNewsImpactHistoryArchiveFromAuthority();
+   return items.map((item) => ({
+     path: getNewsImpactDetailPath(item),
+     lastModified: new Date(item.recordedAt),
+@@ -242,16 +245,23 @@ export function getNewsDetailMetadata(model: NewsDetailPageModel, locale: Conten
+   const isEn = locale === "en";
++  const growth = model.item.growthAuthority;
++  const languageMap: Record<string, string> = isEn
++    ? { "en-US": model.url, "x-default": model.url }
++    : { "fa-IR": model.url, "x-default": model.url };
++  if (model.counterpartUrl) languageMap[isEn ? "fa-IR" : "en-US"] = model.counterpartUrl;
+   return {
+-    title: isEn ? `${model.item.title} | TecPey Crypto News` : `${model.item.title} | اخبار رمزارز تک‌پی`,
+-    description: truncate(model.item.summary, 158),
++    title: growth?.seo.title ?? (isEn ? `${model.item.title} | TecPey Crypto News` : `${model.item.title} | اخبار رمزارز تک‌پی`),
++    description: growth?.seo.metaDescription ?? truncate(model.item.summary, 158),
+     alternates: {
+       canonical: model.url,
+-      languages: {
+-        "fa-IR": isEn ? model.counterpartUrl : model.url,
+-        "en-US": isEn ? model.url : model.counterpartUrl,
+-        "x-default": isEn ? model.counterpartUrl : model.url,
+-      },
++      languages: languageMap,
+     },
+     openGraph: {
+       title: model.item.title,
+@@ -271,6 +281,7 @@ export function buildNewsDetailSchemas(model: NewsDetailPageModel, locale: Conte
+   const isEn = locale === "en";
+   const homeUrl = `${SITE_URL}${localePrefix(locale) || "/"}`;
+   const newsHubUrl = `${SITE_URL}${localePrefix(locale)}/crypto-news`;
++  const growth = model.item.growthAuthority;
+   const about = [
+     ...model.item.relatedCoinSymbols.map((symbol) => ({ "@type": "Thing", name: symbol })),
+     ...model.relatedTools.map((tool) => ({ "@type": "Thing", name: tool.name, url: `${SITE_URL}${localePrefix(locale)}/trading-tools/${tool.slug}` })),
+@@ -318,22 +329,28 @@ export function buildNewsDetailSchemas(model: NewsDetailPageModel, locale: Conte
+       ],
+     },
+     {
+       "@context": "https://schema.org",
+       "@type": "FAQPage",
+-      mainEntity: [
+-        {
+-          "@type": "Question",
+-          name: isEn ? "Is this TecPey news page a trading signal?" : "آیا این صفحه خبر تک‌پی سیگنال معامله است؟",
+-          acceptedAnswer: {
+-            "@type": "Answer",
+-            text: isEn
+-              ? "No. TecPey presents the news as educational market context with source, timing and risk framing."
+-              : "خیر. تک‌پی این خبر را به‌عنوان زمینه آموزشی بازار همراه با منبع، زمان و چارچوب ریسک نمایش می‌دهد.",
+-          },
+-        },
+-        {
+-          "@type": "Question",
+-          name: isEn ? "Why is this news linked to coins or tools?" : "چرا این خبر به کوین یا ابزار وصل شده است؟",
+-          acceptedAnswer: {
+-            "@type": "Answer",
+-            text: isEn
+-              ? "TecPey links supported entities only when the news evidence references them and the relationship passes the content graph rules."
+-              : "تک‌پی فقط زمانی کوین یا ابزار پشتیبانی‌شده را متصل می‌کند که شواهد خبر به آن اشاره کند و رابطه از قواعد گراف محتوا عبور کند.",
+-          },
+-        },
+-      ],
++      mainEntity: growth?.aeo.questions.length
++        ? growth.aeo.questions.map((entry) => ({
++            "@type": "Question",
++            name: entry.question,
++            acceptedAnswer: { "@type": "Answer", text: entry.answer },
++          }))
++        : [
++            {
++              "@type": "Question",
++              name: isEn ? "Is this TecPey news page a trading signal?" : "آیا این صفحه خبر تک‌پی سیگنال معامله است؟",
++              acceptedAnswer: {
++                "@type": "Answer",
++                text: isEn
++                  ? "No. TecPey presents the news as educational market context with source, timing and risk framing."
++                  : "خیر. تک‌پی این خبر را به‌عنوان زمینه آموزشی بازار همراه با منبع، زمان و چارچوب ریسک نمایش می‌دهد.",
++              },
++            },
++          ],
+     },
+   ];
+ }
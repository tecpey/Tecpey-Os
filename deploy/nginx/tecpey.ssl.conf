# TecPey production HTTPS template.
# Replace certificate paths after running: sudo certbot --nginx -d tecpey.ir -d www.tecpey.ir

limit_req_zone $binary_remote_addr zone=tecpey_api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=tecpey_general:10m rate=30r/s;
# SB-014 — credential, session and auth-provider endpoints (login, OTP, 2FA,
# WebAuthn, password change, TOTP, withdrawal authorization) get a zone an
# order of magnitude tighter than tecpey_api. This runs before the request
# reaches Node, so it protects the process from CPU exhaustion (bcrypt, TOTP,
# WebAuthn verification are all expensive per request) independently of the
# app-level Redis-backed limiter, which cannot help if Redis itself is down.
limit_req_zone $binary_remote_addr zone=tecpey_auth:10m rate=1r/s;

server {
    listen 80;
    listen [::]:80;
    server_name tecpey.ir www.tecpey.ir;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name tecpey.ir www.tecpey.ir;

    ssl_certificate /etc/letsencrypt/live/tecpey.ir/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tecpey.ir/privkey.pem;

    client_max_body_size 20M;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript application/xml image/svg+xml;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.tradingview.com https://s3.tradingview.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https: blob:; font-src 'self' data:; connect-src 'self' https: wss:; frame-src 'self' https://www.tradingview.com; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()" always;

    # Regex locations are matched ahead of the plain-prefix /api/ block below
    # (as long as /api/ is not declared with ^~, which it is not), so every
    # request under a credential/session/auth-provider path lands here instead
    # of the looser general API zone, regardless of the order these two blocks
    # appear in.
    location ~ ^/api/(auth/|academy-auth(?:$|/)|academy/auth/|command-center/auth/) {
        limit_req zone=tecpey_auth burst=5 nodelay;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /api/ {
        limit_req zone=tecpey_api burst=20 nodelay;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /_next/static/ {
        proxy_pass http://127.0.0.1:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        limit_req zone=tecpey_general burst=60 nodelay;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

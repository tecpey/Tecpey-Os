<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.App"
        android:supportsRtl="true">

        <!-- Declares the digital asset link this app claims, mirrored at
             https://tecpey.ir/.well-known/assetlinks.json. Verified together
             with the autoVerify intent-filter below. -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name="androidx.browser.trusted.LauncherActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Launcher"
            android:exported="true">

            <meta-data
                android:name="androidx.browser.trusted.EXTRA_PROVIDER"
                android:value="@string/launchUrl" />

            <meta-data
                android:name="androidx.browser.trusted.DEFAULT_URL"
                android:value="@string/launchUrl" />

            <meta-data
                android:name="androidx.browser.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <meta-data
                android:name="androidx.browser.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/colorPrimaryDark" />

            <meta-data
                android:name="androidx.browser.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/splash_background" />

            <meta-data
                android:name="androidx.browser.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="@string/hostName" />
            </intent-filter>
        </activity>

        <!-- Lets the browser hand notification permission/actions back to this
             app once the Digital Asset Link above is verified. -->
        <service
            android:name="androidx.browser.trusted.TrustedWebActivityService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
            </intent-filter>
        </service>

    </application>
</manifest>
